---
- name: Configure Load Balancers
  hosts: loadbalancers
  become: yes
  roles:
    - role: haproxy
  post_tasks:
    - name: Verify HAProxy is running
      service:
        name: haproxy
        state: started
        enabled: yes

# ansible-playbook loadbalacers.yaml